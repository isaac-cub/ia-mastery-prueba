---
import { sanityClient } from 'sanity:client';
import Landing from '../components/Landing.astro';
// Página pública: contenido publicado, SIN stega (limpia, ligera).
const h = await sanityClient.fetch(`*[_type == "home"][0]`);
---
<Landing h={h} />
